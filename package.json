{
  "name": "ecochain-protocol",
  "version": "1.0.0",
  "description": "Protocolo Web3 descentralizado para financiamento e rastreamento de projetos ambientais — EcoChain",
  "author": "Mirtes Fernanda Dutra da Silva",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "interact:local": "hardhat run scripts/interact.js --network hardhat",
    "interact:sepolia": "hardhat run scripts/interact.js --network sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "chai": "^4.3.7",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@openzeppelin/contracts": "5.0.0"
  }
}
